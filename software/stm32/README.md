# STM32

`stm_setting.zip` contains both final STM32CubeIDE projects:

- Sender — ADC sampling, identical channel A/B transmission, CRC/sequence framing, and fault injection.
- Receiver — selected-frame parsing, CRC/sequence/timeout checking, ADC/voltage restoration, and PC-terminal output.
