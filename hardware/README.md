# Hardware

- `ip/` contains the three final custom-IP source packages.
- `vivado/` contains the final SoC block-design notes, Basys3 constraints, and top-level wrapper.

Generated Vivado caches, runs, hardware-manager data, and temporary implementation outputs are excluded.
