# Software

- `vitis/` contains the complete MicroBlaze V workspace snapshot used for AXI configuration, interrupt handling, event logging, and Sensor Guard monitoring.
- `stm32/` contains the two STM32F411RE CubeIDE projects used as Sender and Receiver.

Generated build outputs are retained only inside the archived workspace snapshots when required to preserve the demonstrated state.
